const users = require('./users')
const sessions = require('./sessions')
const games = require('./games')
const gameWeights = require('./games/weight')

module.exports = {
  users,
  sessions,
  games,
  gameWeights
}
