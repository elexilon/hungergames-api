const users = require('./users')
const sessions = require('./sessions')
const games = require('./games')
const gameWeights = require('./games/weights')
const gamePlayers = require('./games/players')

module.exports = {
  users,
  sessions,
  games,
  gameWeights,
  gamePlayers
}
